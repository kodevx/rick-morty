query getLocationById($id: ID!) {
    location(id: $id) {
        id,
        name,
        type,
        dimension,
        residents {
          id
          name
          status
          species
          type
          gender
          image
          created
        }
        created
    }
}
